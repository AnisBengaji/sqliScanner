 Auto-injects payloads into the chosen parameter
- Spoofs headers with random user-agents
- Detects anomalies using content length comparison
# Requirements
  Python 3.x
requests module

install : 
pip install requests
